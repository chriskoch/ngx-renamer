# ============================================================================
# LLM Provider Selection
# ============================================================================
# Choose your LLM provider in settings.yaml (llm_provider: "openai" or "ollama")
# Then configure the corresponding API keys/URLs below:

# ============================================================================
# OpenAI Configuration (required if llm_provider: "openai")
# ============================================================================
# Get your API key from: https://platform.openai.com/settings/organization/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# ============================================================================
# Ollama Configuration (required if llm_provider: "ollama")
# ============================================================================
# URL where Ollama is running
# - Local development: http://localhost:11434
# - Docker on Mac/Windows: http://host.docker.internal:11434
# - Docker on Linux: http://172.17.0.1:11434
# - Remote server: http://your-ollama-server:11434
OLLAMA_BASE_URL=http://localhost:11434

# ============================================================================
# Paperless NGX API Configuration (always required)
# ============================================================================
# Get your API token from your Paperless user profile
PAPERLESS_NGX_API_KEY=your-paperless-api-token-here

# Paperless NGX URL
# IMPORTANT: Must be accessible from within the Docker container
# For docker-compose: Use the webserver service name (e.g., http://webserver:8000/api)
# For external access: Use your domain (e.g., http://paperless.example.com:8000/api)
# MUST include /api at the end
PAPERLESS_NGX_URL=http://webserver:8000/api

# Instructions:
# Method 1: Using .env file (current method)
#   1. Copy this file to .env: cp .env.example .env
#   2. Replace the placeholder values with your actual API keys
#   3. The .env file is git-ignored for security
#
# Method 2: Using docker-compose environment variables (recommended for new installations)
#   - Add these variables directly to your docker-compose.yml or docker-compose.env
#   - See README.md for examples
#   - This method is simpler and doesn't require a .env file in this directory
